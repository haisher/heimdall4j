/** Retry module — configurable retry with fixed/exponential backoff. */
module io.github.haisher.heimdall4j.retry {
    exports io.github.haisher.heimdall4j.retry;
    exports io.github.haisher.heimdall4j.retry.exception;
}
